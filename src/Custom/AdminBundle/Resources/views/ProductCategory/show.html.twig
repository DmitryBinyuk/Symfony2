{% extends "::base-custom-admin.html.twig" %}

{% block content %}
    <div class="container-fluid">
        <div class="container">
            <a class="btn btn-info btn-md" href="{{ path('admin-product-categories') }}">Back</a>
            <ul>
                <li>{{ productCategory.name }}</li>
                <li>{{ productCategory.description }}</li>
            </ul>
        </div>
    </div>
{% endblock %}
