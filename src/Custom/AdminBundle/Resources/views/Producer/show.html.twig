{% extends "::base-custom-admin.html.twig" %}

{% block content %}
    <div class="container-fluid">
        <div class="container">
            <h3>View producer:</h3>
            <a class="btn btn-info btn-md" href="{{ path('admin-producers') }}">Back</a>
            <ul class="list-group">
                <li class="list-group-item">{{ producer.name }}</li>
                <li class="list-group-item">{{ producer.country }}</li>
                <li class="list-group-item">{{ producer.description }}</li>
            </ul>
        </div>
    </div>
{% endblock %}
