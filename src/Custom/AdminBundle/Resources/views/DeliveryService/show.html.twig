{% extends "::base-custom-admin.html.twig" %}

{% block content %}
    <div class="container-fluid">
        <div class="container">
            <h3>View Delivery Service:</h3>
            <a class="btn btn-info btn-md" href="{{ path('admin-delivery-services') }}">Back</a>
            <ul class="list-group">
                <li class="list-group-item">{{ deliveryService.name }}</li>
                <li class="list-group-item">{{ deliveryService.pricePerKilometer }}</li>
            </ul>
        </div>
    </div>
{% endblock %}
