{% extends "::base-custom-admin.html.twig" %}

{% block content %}
    <div class="container-fluid">
        <div class="container">
            <h3>Create Delivery Service:</h3>
            <a class="btn btn-info btn-md" href="{{ path('admin-delivery-services') }}">Back</a>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
